# Glory POS Application Requirements
# Install all dependencies with: pip install -r requirements.txt

# GUI Framework
PyQt5>=5.15.0

# HTTP Requests (for cloud API and device communication)
requests>=2.28.0

# Image Processing (for PyInstaller icon handling)
Pillow>=9.0.0

# Build Executable (development only)
PyInstaller>=6.0.0
